{
  "name": "sportsball",
  "version": "0.1.0",
  "description": "Sportsball — the FIFA World Cup 2026 in your terminal: schedule, favorites, live streaming, highlights, stats, and AI analysis.",
  "type": "module",
  "bin": {
    "sportsball": "src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile dist/sportsball",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.9.2",
    "bun-types": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
